---
type: brief
id: Final
title: "Cut 1.3.1 + dismiss ghost alerts #6/#7 (runs LAST)"
status: ready
effort: low
wave: Final
depends_on: ["B1", "B2", "B3"]
touches:
  - Cargo.toml
  - Cargo.lock
  - CHANGELOG.md
forbidden:
  - "src/**, tests/**  (no feature/behavior/test changes in the cut)"
  - "the centaur_technical_indicators dependency line  (stays 1.3.0 — binding-only patch)"
  - "pyproject.toml author/maintainer email  (deferred again — address still does not exist)"
  - "test_requirements.txt (B2) / AGENTS.md (B3) / .github/workflows/**"
branch: "chore/cut-1.3.1"
pr_target: "main"
changelog: "promote [Unreleased] → [1.3.1] (do not append)"
related:
  - "[[PLAN-1.3.1]]"
decisive_test: "Final-A1"
created: 2026-06-21
tags: [brief, release]
---

# Final — Cut 1.3.1 + dismiss ghost alerts #6/#7 (runs LAST)

> **Self-contained.** Repo conventions live in `AGENTS.md` / `CLAUDE.md`. **Done for this session =
> the version-bump PR is merged to `main`, green, with `cti.__version__ == "1.3.1"` confirmed, the
> tag step handed off, and the ghost alerts dismissed.** The PyPI publish is **irreversible and
> auto-triggered by the tag** — **do not push the tag yourself; hand it off.** Stop and report on any
> blocker.

## Mission

Finalize 1.3.1: bump the package version, promote the changelog, run the gates, confirm
`cti.__version__ == "1.3.1"`, then **hand the tag/publish to the maintainer**. After the cut is on
`main`, **dismiss the two ghost Dependabot alerts** (#6/#7) that the test-dep bump cannot clear.

- **`[package] version` `1.3.0` → `1.3.1`** (binding-only patch). The `centaur_technical_indicators`
  **core dependency stays `1.3.0`** — a deliberate divergence from the version-match rule, now
  governed by B3's `AGENTS.md` note.

## Context

- **Tag-triggered, auto-publishing release.** `.github/workflows/CI.yml`'s `release` job enters on
  `refs/tags/*`, needs `verify` + the linux/musllinux/windows/macos wheel jobs + `sdist`, and runs
  `uv publish` (token `UV_PUBLISH_TOKEN` ← `secrets.PYPI_API_TOKEN`) — **automatically, with no
  manual in-CI gate** (`CI.yml` ~L191–213). So **pushing the `v1.3.1` tag *is* the publish
  decision.** Tag convention is `vX.Y.Z` → **`v1.3.1`**.
- **`[package] version` drives `cti.__version__`** via the 1.3.0 mixed-layout `importlib.metadata`
  resolution — so the single version edit updates both the wheel version and `cti.__version__`.
- Past releases **rename** `## [Unreleased]` straight to the version (no empty `[Unreleased]` kept
  afterward — see `## [1.2.2]`). Follow that pattern.
- The author/maintainer **email** placeholder (`your@email.com`) in `pyproject.toml` stays
  **deferred** (the address still does not exist; no-PII rule). Do not touch it.

## Prerequisites (confirm; do not perform here)

- **B1 merged:** `Cargo.toml` has `pyo3 = "0.29"` + `rust-version = "1.83"`; `CHANGELOG.md`
  `## [Unreleased]` carries the PyO3 `### Security` (+ MSRV `### Changed`) entries.
- **B2 merged:** `test_requirements.txt` has `pytest==9.0.3` / `Pygments==2.20.0`; the pytest/Pygments
  `### Security` content is under `## [Unreleased]`. Ideally B2 has been on `main` long enough for
  Dependabot to resolve alerts **#2/#3**.
- **B3 merged:** `AGENTS.md` source-of-truth note refreshed (the divergence is documented).
- All three merged and `## [Unreleased]` fully populated. If anything is missing, **stop**.

## Verify first (re-confirm at session start)

| Claim | How to check | Expected |
|-------|--------------|----------|
| package version lags, dep current | `grep -nE '^version\|centaur_technical_indicators =' Cargo.toml` | `version = "1.3.0"`, dep `= "1.3.0"` |
| pyo3 already bumped (B1 in) | `grep -n 'pyo3' Cargo.toml` | `pyo3 = "0.29"` |
| `[Unreleased]` populated | `sed -n '/## \[Unreleased\]/,/## \[1.3.0\]/p' CHANGELOG.md` | Security (+ Changed) content present |
| tag convention | `git tag --sort=-v:refname \| head` | `v`-prefixed (`v1.3.0`, …) |
| ghosts still open on requirements.txt | `for n in 6 7; do gh api .../dependabot/alerts/$n --jq '{number,state,manifest:.dependency.manifest_path}'; done` | `open`, `requirements.txt` |

## Changes (in order)

1. **`Cargo.toml` — bump `[package] version` `"1.3.0"` → `"1.3.1"`.** Leave the dependency at
   `1.3.0`. Nothing else.
2. **`Cargo.lock`** — regenerated by the next build (this package's own version updates). Stage it.
3. **`CHANGELOG.md` — promote, don't append.** Rename `## [Unreleased]` to
   `## [1.3.1] - <YYYY-MM-DD>` (today), keeping the consolidated `### Security` / `### Changed` as-is.
   Leave **no** stray empty `[Unreleased]`.
4. **Build + confirm version.** `maturin develop`, then
   `python -c "import centaur_technical_indicators as c; print(c.__version__)"` → **must be
   `1.3.1`**. If it still prints `1.3.0`, the editable dist-info is stale — reinstall and re-check;
   if it persists, **stop and report**.
5. **Gates:** `python -m pytest` (full suite green), `cargo fmt --all -- --check` clean.
6. **PR (`chore/cut-1.3.1` → `main`), merge.**
7. **HAND OFF THE TAG — do not push it.** Tell the maintainer: tag the merged commit `v1.3.1` and
   push it; CI's `release` job builds all-platform wheels + sdist and runs `uv publish` to PyPI
   automatically. This is the irreversible publish — the maintainer's action, not this session's.
8. **Dismiss the ghost alerts (#6/#7) — after the cut is on `main`.** These are mis-attributed to
   `requirements.txt` (which lists only `maturin`) and the B2 bump cannot clear them. **Re-query
   first** (only dismiss those still `open` on `requirements.txt`), then PATCH:

   ```
   for n in 6 7; do gh api /repos/chironmind/CentaurTechnicalIndicators-Python/dependabot/alerts/$n \
     --jq '{number,state,manifest:.dependency.manifest_path,dep:.dependency.package.name}'; done

   gh api --method PATCH -H "Accept: application/vnd.github+json" \
     /repos/chironmind/CentaurTechnicalIndicators-Python/dependabot/alerts/7 \
     -f state=dismissed -f dismissed_reason=inaccurate \
     -f dismissed_comment="Stale dependency-graph node: requirements.txt lists only maturin==1.11.5; pytest is not present (removed at the Rust refactor). Genuine pytest advisory tracked on test_requirements.txt (#3), fixed by the 9.0.3 bump in 1.3.1."

   gh api --method PATCH -H "Accept: application/vnd.github+json" \
     /repos/chironmind/CentaurTechnicalIndicators-Python/dependabot/alerts/6 \
     -f state=dismissed -f dismissed_reason=inaccurate \
     -f dismissed_comment="Stale dependency-graph node: requirements.txt lists only maturin==1.11.5; Pygments is not present. Genuine Pygments advisory tracked on test_requirements.txt (#2), fixed by the 2.20.0 bump in 1.3.1."
   ```

   Requires a token with **security-events write** (or `repo`) scope + admin/security-manager rights.
   If the token here lacks it, **hand the two commands to the maintainer** instead of forcing it.
9. **Final housekeeping (only after publish + dismissal confirmed):** remove untracked scratch
   (`*_BRIEF.md`, `PLAN-1.3.1.md` if treated as scratch) — or, mirroring 1.3.0, preserve the plan +
   briefs as a `docs:` knowledge-base commit. Confirm with the maintainer which (1.3.0 preserved
   them). Plain `rm` of never-committed files is not a git change.

## Acceptance tests (named; all must pass)

- **Final-A1 (decisive)** — after step 4, `cti.__version__ == "1.3.1"`.
- **Final-A2** — `Cargo.toml [package] version == "1.3.1"`, dependency `centaur_technical_indicators
  == "1.3.0"`; `CHANGELOG.md` top release section is `## [1.3.1] - <date>` with no leftover
  `[Unreleased]` content.
- **Final-A3 (suite)** — `maturin develop` clean, `python -m pytest` green, `cargo fmt --all --
  check` clean; no new dependency; only `Cargo.toml` / `Cargo.lock` / `CHANGELOG.md` changed.
- **Final-A4 (post-merge — maintainer)** — `v1.3.1` tag pushed; CI `release` job green; PyPI shows
  `centaur_technical_indicators 1.3.1`. *(Verification, not a step this session performs.)*
- **Final-A5 (post-publish — alerts)** — **all six Dependabot alerts closed**: #4/#5 resolved by B1,
  #2/#3 by B2, #6/#7 dismissed. `gh api .../dependabot/alerts --jq '[.[]|select(.state=="open")]|length'`
  → **0**.

## Out of scope / Do NOT

- Any `src/**` / `tests/**` change; the author/maintainer **email** (deferred); `test_requirements.txt`
  (B2), `AGENTS.md` (B3), `[tool.maturin]`, `docs/` shipping config, CI workflows.
- **Do NOT** bump the core-crate dependency (stays `1.3.0`).
- **Do NOT** push the tag / publish — maintainer-only. No `uv publish`, `maturin publish`, or
  `git push --tags`.
- **Do NOT** dismiss #6/#7 before B2 is on `main` (let #2/#3 resolve first) or without the
  pre-dismiss re-query.

## Definition of done

- [ ] Final-A1 (`cti.__version__ == "1.3.1"`), A2, A3 green. **(These three gate *this* session.)**
- [ ] `## [Unreleased]` promoted to `## [1.3.1] - <date>`; only `Cargo.toml` / `Cargo.lock` /
      `CHANGELOG.md` changed; nothing in `forbidden` moved.
- [ ] PR opened against `main`; **tag/publish handed to the maintainer**, not performed.
- [ ] Ghost alerts #6/#7 dismissed (or commands handed off); Final-A5 confirmed post-publish (0 open).
- [ ] Final-A4 (post-merge, **maintainer**): `v1.3.1` tag pushed; CI `release` green; PyPI shows 1.3.1.
- [ ] (After publish) scratch removed, or plan+briefs preserved as a docs knowledge-base commit
      (maintainer's call).

## Report (per AGENTS.md)

Summary · Scope (release only — `Cargo.toml`/`Cargo.lock`/`CHANGELOG.md`) · Compatibility (release
only — no API change; note the deliberate `[package] 1.3.1` vs core `1.3.0` divergence) · Validation
(gate output + the `cti.__version__` value) · Changelog (promoted `[Unreleased]` → `[1.3.1]`) — plus
Final-A1–A3 results, the explicit `v1.3.1` tag hand-off note, the ghost-alert dismissal outcome
(or hand-off), and anything flagged.
